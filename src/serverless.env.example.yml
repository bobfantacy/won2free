TG_TOKEN: #Use BotFather generate your won tg bot
LAYER_NAME: won2free-python-requirements:1
MINIAPP_URL: https://your-domain/#/
JWT_SECRET: #Use your secret key

QUEUE_NAME: won2free.fifo
dev_schedule1: cron(0 0 ? * * *)
dev_schedule2: cron(0 0 ? * * *)
schedule_daily: cron(1 8 ? * * *)
prod_schedule1: cron(0/1 * ? * * *)
prod_schedule2: cron(0/1 * ? * * *)